[package]
name = "fledge-plugin-quiz"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "quiz"
path = "src/main.rs"

[dependencies]
crossterm = "0.28"
rand = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
strip = true
lto = true
opt-level = "z"
